# Static assets

Place the company overview video here for the **Media** page:

- **Filename:** `MR DGN Group.mp4` (exact spelling and spaces)
- The Media page uses path `/assets/MR DGN Group.mp4`. If you use a different filename, update the constant in `src/pages/Media.tsx`.
