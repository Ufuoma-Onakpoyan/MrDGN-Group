# Database - use Neon (or any PostgreSQL) connection string
# Get this from Neon dashboard: Connection string (pooled)
DATABASE_URL="postgresql://user:password@host.neon.tech/neondb?sslmode=require"

# JWT secret - generate with: openssl rand -base64 32
JWT_SECRET="your-super-secret-jwt-key-change-in-production"

# Server
PORT=3001
NODE_ENV=development

# CORS - comma-separated origins (e.g. http://localhost:5173,https://yoursite.com)
CORS_ORIGINS="http://localhost:5173,http://localhost:5174,http://localhost:5175,http://localhost:5176,http://localhost:5177"

# Uploads - base URL for served files (e.g. http://localhost:3001/uploads)
UPLOAD_URL="http://localhost:3001/uploads"

# Optional: public origin for API and uploads (used when returning absolute image URLs to frontends)
# If not set, derived from UPLOAD_URL or API URL. Set in production to your backend URL.
# API_ORIGIN="https://api.yourdomain.com"

# Seed - first admin (run: npm run db:seed)
SEED_ADMIN_EMAIL="admin@mrdgngroup.com"
SEED_ADMIN_PASSWORD="Admin123!"
